//just need this for loading state, transitions, and themeing depending on view
import classNames from 'classnames';
export const masterContainer = classNames({
'masterContainer': true
// ,'loadingPhase': true

});
